// TODO Build new actions types and function action types

// type ExampleActionType = {
//   type: 'EXAMPLE_ACTION';
//   payload: boolean;
// };

// export type ExampleActionTypeActionFnType = (token: boolean) => ExampleActionType;

//////////////////////////////
// ? Global Export all actions
export type GeneralActionType = {};
